// this file is added jsut for testing git merge and push
export class Person {
  public name: string;
  constructor(public name: string){this.name = name;}
  greet(){
    return "Hello, "+this.name;
  }
}
